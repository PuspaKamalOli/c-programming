// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int main()
{
    int a;
    printf("enter any number: ");
    scanf("%d", &a);
    a = pow(a, 2);
    printf("the square is %d", a);

    return 0;
}